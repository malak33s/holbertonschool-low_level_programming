#include"3-calc.h"

/**
 * op_add - returns the sun of integers.
 * @a: first integer.
 * @b: second integer.
 * Return: sum of add.
 */
int op_add(int a, int b)
{
	return (a+b);
}
/**
 * op_sub -nreturns the difference of the integers.
 * @a: first integers
 * @b: second integers
