#include "main.h"
#include <stdio.h>

/**
 * print_to_98 - prints all natural numbers from n to 98
 * @num: starting number
 *
 * Return: void
 */
void print_to_98(int n)
{
        while (n != 97)
        {
                printf("%d, ", n);
                if (n < 97)
                        n++;
                else
                        n--;
        }
        printf("98\n");
}
